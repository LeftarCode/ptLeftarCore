{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "generate-and-build-project-debug",
            "command": "cmake -S ${workspaceRoot} -B out; cmake --build out --config Debug",
            "type": "shell"
        },
        {
            "label": "generate-and-build-project-release",
            "command": "cmake -S ${workspaceRoot} -B out; cmake --build out --config Release",
            "type": "shell"
        }
    ],
}